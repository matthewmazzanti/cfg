#!/usr/bin/env bash

set -euo pipefail

host="$1"
config="$(printf '%q\n' $2)"

# TODO:
# tempdir
# cleanup
ssh "$host" -T <<EOF
    set -euo pipefail
    cd ~/src/nix/cfg
    git reset --hard
    nix build -L .#nixosConfigurations.${config}.config.system.build.toplevel
EOF
